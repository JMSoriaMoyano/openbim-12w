# =============================================================================
# Dependencias del plan OpenBIM 12 semanas — fijadas en S3·X (28/05/2026)
# Python 3.14 · venv .venv/ · IfcOpenShell vía pip (no conda)
# Reproducir entorno: pip install -r requirements.txt
# =============================================================================

# --- Runtime: IfcOpenShell + transitivas (pip freeze tras pip install ifcopenshell==0.8.5)
ifcopenshell==0.8.5
isodate==0.7.2
lark==1.3.1
numpy==2.4.6
python-dateutil==2.9.0.post0
shapely==2.1.2
six==1.17.0
typing_extensions==4.15.0

# --- Desarrollo y testing (se instalarán cuando lo necesitemos)
# pytest>=8.0
# pytest-cov>=5.0

# --- Notebooks (S4·L+ si decidimos usarlos)
# jupyter>=1.0
# nbconvert>=7.0

# --- Procesado tabular / reporting (S6·L validación)
# pandas>=2.2
# matplotlib>=3.8

# --- Parsing IDS (S7·L)
# lxml>=5.0

# --- Plantillas y CLI (utilidades)
# jinja2>=3.1
# rich>=13.7
# click>=8.1
